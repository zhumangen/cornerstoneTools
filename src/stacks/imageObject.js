/**
 * Creates an Image Object
 *
 * @param images
 * @param options
 * @constructor
 */
export class ImageObject {
  constructor (images, options) {
    this.images = images;
    this.options = options;
  }
}
